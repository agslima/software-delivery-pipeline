diff --git a/node_modules/eslint/lib/shared/ajv.js b/node_modules/eslint/lib/shared/ajv.js
index 2ae6a07..7f6c2b7 100644
--- a/node_modules/eslint/lib/shared/ajv.js
+++ b/node_modules/eslint/lib/shared/ajv.js
@@ -9,8 +9,8 @@
 // Requirements
 //------------------------------------------------------------------------------
 
-const Ajv = require("ajv"),
-	metaSchema = require("ajv/lib/refs/json-schema-draft-04.json");
+const Ajv = require("ajv");
+const addDraft4 = require("ajv-draft-04");
 
 //------------------------------------------------------------------------------
 // Public Interface
@@ -18,19 +18,16 @@ const Ajv = require("ajv"),
 module.exports = (additionalOptions = {}) => {
-	const ajv = new Ajv({
-		meta: false,
-		useDefaults: true,
-		validateSchema: false,
-		missingRefs: "ignore",
-		verbose: true,
-		schemaId: "auto",
-		...additionalOptions,
-	});
-
-	ajv.addMetaSchema(metaSchema);
-	// eslint-disable-next-line no-underscore-dangle -- Ajv's API
-	ajv._opts.defaultMeta = metaSchema.id;
-
-	return ajv;
+	const ajv = new Ajv({
+		useDefaults: true,
+		validateSchema: false,
+		strict: false,
+		strictSchema: false,
+		...additionalOptions,
+	});
+
+	addDraft4(ajv);
+	addDraft4.defaultMeta(ajv);
+
+	return ajv;
 };
