apiVersion: apps/v1
kind: Deployment
metadata:
  name: unsigned-app
  namespace: production
spec:
  selector:
    matchLabels:
      app: bad
  template:
    metadata:
      labels:
        app: bad
    spec:
      containers:
        - name: app
          image: docker.io/library/nginx:latest
